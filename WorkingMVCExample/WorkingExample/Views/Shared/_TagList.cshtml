@using WorkingExample.Models
 <ul class="dropdown-menu">
     @foreach(var tag in TagHelper.Tags())
     {
         <li>@Html.ActionLink(tag, "ByTag", "Home", new { tagName = tag}, new {})</li>
     }
 </ul>